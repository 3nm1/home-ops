---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: EnvoyExtensionPolicy
metadata:
  name: seerr-themepark
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: seerr
  lua:
    - type: ValueRef
      valueRef:
        group: v1
        kind: ConfigMap
        name: seerr-themepark-lua
